from reflect_html import *
from reflect_antd import Table
from reflected.react.resizable import Resizable
onResize, width, ...restProps = props.onResize, props.width, props....restProps
def app():
    return[
 Resizable(width=width, height=0, handle="{         <span           className=", react-resizable-handle"=True, onClick="{lambda e:", {=True, e.stopPropagation();=True, !}"=True),
 th("{...restProps}"=True),
]
def app():
    return Demo()