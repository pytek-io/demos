from render_html import *
from render_antd import Table, Tag, Radio, Space
def app():
    return Tag(""{tag.toUpperCase()}"", color=color, key=tag)
def app():
    return div([div(Radio.Group(style=dict(marginBottom=10), options=topOptions, value=this.state.top, onChange="{lambda e:", {=True, this.setState(=True, top:=True, e.target.value=True, );=True, !}"=True)), Radio.Group(style=dict(marginBottom=10), options=bottomOptions, value=this.state.bottom, onChange="{lambda e:", {=True, this.setState(=True, bottom:=True, e.target.value=True, );=True, !}"=True), Table(columns=columns, pagination=dict(position=[this.state.top), dataSource=data)])
def app():
    return Demo()