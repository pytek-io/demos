from render_html import *
from render_antd import Modal, Button, Space
def app():
    return[
 ReachableContext.Consumer(""{namlambda e:`Reachable: $"{name}"!`}""),
 br(),
 UnreachableContext.Consumer(""{namlambda e:`Unreachable: $"{name}"!`}""),
]
def app():
    return ReachableContext.Provider([Space([Button("Confirm", onClick="{lambda :", {=True, modal.confirm(config);=True, !}"=True), Button("Warning", onClick="{lambda :", {=True, modal.warning(config);=True, !}"=True), Button("Info", onClick="{lambda :", {=True, modal.info(config);=True, !}"=True), Button("Error", onClick="{lambda :", {=True, modal.error(config);=True, !}"=True)]), ""{/* `contextHolder` should always under the context you want to access */}"       "{contextHolder}"        "{/* Can not access this context since `contextHolder` is not in it */}"", UnreachableContext.Provider(value="Bamboo")], value="Light")
def app():
    return App()