from render_antd import Button, Form, Input, Modal, Radio
from render_html import *


def app(_):
    return Modal([""{             form.resetFields();             onCreate(values);           )           .catch(info => "{             print('Validate Failed:', info);           );       !}"     >", Form([Form.Item(Input(), name="title", label="Title", rules="{[", {=True, required:=True, true,=True, message:=True, 'Please=True, input=True, the=True, title=True, of=True, collection!'=True, }"]}"=True), Form.Item(Input(type="textarea"), name="description", label="Description"), Form.Item(Radio.Group([Radio("Public", value="public"), Radio("Private", value="private")]), name="modifier", className="collection-create-form_last-form-item")], form=form, layout="vertical", name="form_in_modal", initialValues=dict(modifier='public'))], visible=visible, title="Create a new collection", okText="Create", cancelText="Cancel", onCancel=onCancel, onOk="{lambda :", {=True, form=True, .validateFields()=True, .then(values="")
onCreate = div([Button("New Collection", type="primary", onClick="{lambda :", {=True, setVisible(true);=True, !}"=True), CollectionCreateForm(visible=visible, onCreate=onCreate, onCancel="{lambda :", {=True, setVisible(false);=True, !}"=True)])
def app(_):
    return CollectionsPage()