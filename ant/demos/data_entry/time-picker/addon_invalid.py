from reflect_html import *
from reflect_antd import TimePicker, Button
def app():
    return TimePicker("Ok", open=this.state.open, onOpenChange=this.handleOpenChange, renderExtraFooter="{lambda :(           <Button size=", small"=True, type="primary", onClick=this.handleClose)
def app():
    return TimePickerAddonDemo()